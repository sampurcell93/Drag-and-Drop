script(type='text/template')#data-type
    div.hide-properties.fl.w6
        span.p10.icon-uniF48A &nbsp;
        |<%= name %>
    span.add-property.fr.clickme  Add Property + 
    .clear
script(type='text/template')#property-item
    <% if (typeof editable !== "undefined" && editable == true) { %>
    <div contentEditable>
    <% } else {%>
    <div>
    <% } %>
    //- Sorry for the shitty formatting - jade is bad at indents.
    | <%= name.clean() %>
    </div>
    label
        input.fr.choose-prop(type='checkbox')
//- Template to edit properties
script(type='text/template')#property-item-editor
    <%= className + "." + name.clean() %>
script(type='text/template')#generic-element
    <%= type %>
script(type='text/template')#settings-template
    label.m10 History Length:
    div.m10.hist-length(data-setting='history_length')
    p.nice-dialog 
        strong Be careful: 
        | Changing the length of your builder's history can cause performance issues, especially when multiple tabs are open.